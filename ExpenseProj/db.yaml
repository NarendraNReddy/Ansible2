- name: DB
  hosts: localhost
  become: yes
  tasks:
  - name: install mysql-server
    ansible.builtin.dnf:
      name: mysql-server
      state: latest
  - name: start mysql service
    ansible.builtin.service:
      name: mysqld
      state: started
      enabled: yes

